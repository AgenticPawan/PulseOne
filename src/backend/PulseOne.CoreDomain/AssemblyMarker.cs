namespace PulseOne.CoreDomain;

/// <summary>
/// Assembly marker for PulseOne.CoreDomain. Business aggregates and value objects
/// are added here in later phases (Phase 2+). Kept so the project compiles in Phase 0.
/// </summary>
public static class AssemblyMarker;
