namespace PulseOne.Application;

/// <summary>
/// Assembly marker for PulseOne.Application. CQRS commands/queries and handlers
/// (records, per CLAUDE.md) are added here in Phase 2. Kept so the project compiles in Phase 0.
/// </summary>
public static class AssemblyMarker;
